apiVersion: v1
kind: Service
metadata:
 name: svc-rabbit
spec:
 type: NodePort
 ports:
  - port: 81
    targetPort: 15672
    nodePort: 30072
    protocol: TCP
    name: mgmtguiport
  - port: 80
    targetPort: 5672
    protocol: TCP
    name: apiport
 selector:
  app: rabbitmq